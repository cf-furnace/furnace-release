---
<<: (( merge ))

properties:
  <<: (( merge ))
  capi:
    <<: (( merge ))
    nsync:
      <<: (( merge ))
      kube: (( config_from_kube.nsync ))
    tps:
      <<: (( merge ))
      kube: (( config_from_kube.tps ))
  furnace:
    <<: (( merge ))
